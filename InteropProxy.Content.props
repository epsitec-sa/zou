<?xml version="1.0" encoding="utf-8"?>
<!--
The InteropProxy.props can be used to import C++ project outputs into C# interop projects.
-->
<Project ToolsVersion="4.0" InitialTargets="InteropProxyContentTrace" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- Set default metadata -->
  <ItemDefinitionGroup>
    <Content>
      <Link Condition="'%(Link)' == ''">%(RecursiveDir)%(FileName)%(Extension)</Link>
      <CopyToOutputDirectory Condition="'%(CopyToOutputDirectory)' == ''">PreserveNewest</CopyToOutputDirectory>
      <Visible Condition="'%(Visible)' == ''">false</Visible>
    </Content>
  </ItemDefinitionGroup>

  <!-- ================ InteropTrace ================ -->
  
  <Target Name="InteropProxyContentTrace">
    <Message Text="%(Content.Identity) -&gt; %(Link)" />
  </Target>
</Project>