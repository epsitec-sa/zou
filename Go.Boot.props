<!-- Import this script at the top of a C# agent -->
<Project>
  
  <PropertyGroup>
    <GoBootImported>true</GoBootImported>
  </PropertyGroup>

  <Import Project="zou.props" />
  <Import Project="zou.Errors.props" />

  <ImportGroup Label="ExtensionSettings">
    <Import Project="Cs.PlatformToolset.props" />
    <Import Project="Boot.Core.props" />
  </ImportGroup>
</Project>