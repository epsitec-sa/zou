usage: git vtable [<options>]

Create a version table in markdown format.

    -h|--help               display help
    -j<n>                   number of submodules processed in parallel (default to 16)
    
example:
    git vtable              # create versions-<branch>.md
