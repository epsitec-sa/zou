usage: git vtable [<options>]

Create a version table in markdown format (versions.md).

    -h|--help               display help
    -j<n>                   number of submodules processed in parallel (default to 32)
    -c|--commit             commit vtable
    
example:
    git vtable              # create a vtable
    git vtable -c           # create a vtable and commit it
