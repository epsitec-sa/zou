usage: git prunetags [<options>]

Prune tags

    -h|--help               display help
    .                       visit startup module only (do not visit submodules)
    -g|--global             visit all registered bundles recursively (see git bundler)
    <git-for options>       options for 'git for'
    
example:
    git prunetags           # prune tags recursively
    git prunetags .         # prune tags in startup module only
    git prunetags -g        # prune tags in all registered bundles recursively
