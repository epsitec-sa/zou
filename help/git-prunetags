usage: git prunetags [<options>]

Prune tags - delete local tags and fetch remote tags.
Opposite operation: 'git mirrortags'.

    -h|--help               display help
    -r|--recursive          visit specified modules recursively
    -a|--all                visit all submodules (skip SKU filter)
    -v|--verbose            display tags
    -g|--global             visit all registered bundles recursively (see git bundler)
    
example:
    git prunetags           # prune tags
    git prunetags -r        # prune tags recursively
    git prunetags -g        # prune tags in all registered bundles recursively
