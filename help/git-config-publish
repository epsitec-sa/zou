git config-publish [<options>] - Configure publishing

    -h|--help               display help
    --enable                enable  publish of current branch
    --disable               disable publish of current branch
    
example:
    git config-publish              # display status
    git config-publish --disable    # disable publish for current branch
