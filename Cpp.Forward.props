<!--
Import this script at the beginning of a C++ forwarding agent.

Examples:
  For an importation project:
    <Import Project="zou\Cs.Forward.props" />
    <Import Project="$(ZouDir)Import.targets" />
  For an packaging project:
    <Import Project="zou\Cs.Forward.props" />
    <Import Project="$(ZouDir)Pack.targets" />
-->
<Project>
  <PropertyGroup>
    <SkipCrossBuild>true</SkipCrossBuild>
  </PropertyGroup>
  <Import Project="Cpp.Agent.props" />
</Project>
