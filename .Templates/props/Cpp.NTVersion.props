<?xml version="1.0" encoding="utf-8"?>
<!--
0x0400 - Windows NT 4.0
0x0500 - Windows 2000
0x0501 - Windows XP
0x0502 - Windows Server 2003
0x0600 - Windows Vista
0x0601 - Windows 7

Replace 'id' in '_PropertySheetDisplayName' content below with:
- 'name of bundle'   if this template is saved in   'bundle-dir'/zou.cfg
- 'name of solution' if this template is saved in 'solution-dir'/zou.cfg
- 'name of project'  if this template is saved in  'project-dir'/zou.cfg

-->
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <_PropertySheetDisplayName>[id] NTVersion = 0x600 (Windows Vista)</_PropertySheetDisplayName>
  </PropertyGroup>
  
  <PropertyGroup Condition="'$(NTVersion)' == ''">
    <NTVersion>0x0600</NTVersion>
  </PropertyGroup>
</Project>