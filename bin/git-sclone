#!/usr/bin/env bash

. zoul-git-core

args=()
while [[ "$#" > 0 ]]; do case "$1" in
  -h|--help) zou-help $0; exit 0;;
  *) args+=("$1");;
esac; shift; done

git_clone --recurse-submodules -j8 "${args[@]}"
