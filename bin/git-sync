#! /bin/bash

. zlib-git-branch

while [[ "$#" > 0 ]]; do case "$1" in
  -h|--help) zou-help $0; exit 0;;
  --ignore-submodules) ISM=$1;;
  -*) echo "unknown option: $1" >&2; exit 1;;
  *) ;;
esac; shift; done

branch_sync_all