#! /bin/bash

. zlib-git-branch

while [[ "$#" > 0 ]]; do case "$1" in
  -h|--help) zou-help $0; exit 0;;
  -0|--ignore-submodules) SYNC_ISM=true;;
  -*) echo "unknown option: $1" >&2; exit 1;;
  *) ;;
esac; shift; done

branch_sync_all