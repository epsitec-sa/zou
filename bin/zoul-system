#!/bin/bash

# Use the following statement to include current lib only once
#   [[ -z $ZOUL_SYSTEM ]] && . zoul-system
ZOUL_SYSTEM=true

real_path()
{
  # TODO: adapt for Mac
  realpath "$1"
}

sys_linux()   { [ $(uname) = 'Linux' ]; }
sys_winbash() { [[ $(uname) =~ ^MINGW ]]; }
sys_wsl()     { sys_linux && [[ "$(< /proc/version)" =~ .*-microsoft- ]]; }