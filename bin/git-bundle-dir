#! /bin/sh

dir=.

while [[ "$#" > 0 ]]; do case "$1" in
  -h|--help) zou-help $0; exit 0;;
  -*) echo "unknown option: $1" >&2; exit 1;;
  *) dir=$1; break;;
esac; shift; done

dir=$(readlink -f $dir)
while [[ -n "$dir" && ! -d "$dir/.git" ]]; do
  dir=${dir%/*}
done

echo $dir
