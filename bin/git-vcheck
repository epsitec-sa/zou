#! /bin/bash

. zlib-git-vtag

while [[ "$#" > 0 ]]; do case "$1" in
  -h|--help) zou-help $0; exit 0;;
  --debug) debug=true;;
  -*) echo "unknown option: $1" >&2; exit 1;;
  *) vtag=$1;;
esac; shift; done

[[ -n $vtag ]] &&  vtag_check $vtag || vtag_check_all