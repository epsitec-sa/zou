#!/bin/bash

. zoul-git-repo

# TODO: add an option to force remove dangling submodules
repo_require_clean_work_tree 'cannot clean'
repo_remove_dangling_submodules

[ -d node_modules ] && rm -rf node_modules
git clean -xdf -e packages
git checkout .
