#! /bin/sh

"!f() { local version=$1; local regex; if [[ -z "$version" ]]; then regex=[0-9]; else regex=$(echo $version | sed s,[.],\.,g); fi; git tag -l --sort=-v:refname | grep ^v$regex || true; }; f"
