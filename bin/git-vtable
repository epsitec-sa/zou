#!/usr/bin/env bash

. zoul-core

vtable_show()
{
  local obj="${1:-HEAD}"
  gitx notes --ref 'zou-vtable' show "$obj^{}" 2>/dev/null \
  || io_message "No version table found at ${VC}$obj${Red}" '' ${Red}
}

vtable_show "$@"