<Project DefaultTargets="Build">
  <Import Project="..\Cs.Pack.props" />

  <PropertyGroup>
    <PkgBinDir>$(ZouBinzDir)tasks\</PkgBinDir>
  </PropertyGroup>

  <ItemGroup>
    <SignFolder Include="$(PkgBinDir)win-x64" />
  </ItemGroup>

  <Import Project="Tasks.Build.targets" />
  <Import Project="$(ZouDir)Cs.Pack.targets" />
</Project>
