<Project DefaultTargets="Build">
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <OutDir>$(MSBuildProjectDirectory)/bin/redist/</OutDir>
  </PropertyGroup>
  <Import Project="..\Cs.Import.props" />

  <Import Project="Tasks.redist.props" />

  <Import Project="$(ZouDir)Cs.Import.targets" />
</Project>
