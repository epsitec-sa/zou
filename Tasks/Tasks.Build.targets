<Project InitialTargets="TraceTasksRedist">
  <ItemGroup>
    <ImportProject Include="Tasks.csproj">
      <Targets>Publish</Targets>
    </ImportProject>
  </ItemGroup>

  <Target Name="TraceTasksRedist" Condition="'$(ZouTrace)' == 'true'">
    <LogItems Items="@(ImportProject)" />
  </Target>
</Project>
