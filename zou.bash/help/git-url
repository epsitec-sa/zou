usage: git url [<options>] <path>

Display remote origin URL (git config remote.origin.url)

    -h|--help               display help
    <path>                  path to git repo / submodule
    
example:
    git url                 display URL of current repo / submodule
    git url PATH            display URL of PATH    repo / submodule
