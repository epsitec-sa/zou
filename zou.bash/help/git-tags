usage: git tags [<options>]

Tags helpers (list, sync, prune and mirror)

    -h|--help               display help
    --sync                  pull remote tags and push local tags
    --prune                 remove local tags which are not remote
    --mirror                remove remote tags which are not local
    
example:
    git tags                # list tags
    git tags --sync         # synchronize tags
