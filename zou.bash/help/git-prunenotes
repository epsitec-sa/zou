usage: git prunenotes [<options>]

Prune notes - delete local notes and fetch remote notes.

    -h|--help               display help
    -r|--recursive          visit specified modules recursively
    -a|--all                visit all submodules (skip SKU filter)
    -v|--verbose            display removed notes
    -g|--global             visit all registered bundles recursively (see git bundler)
    
example:
    git prunenotes           # prune notes
    git prunenotes -r        # prune notes recursively
    git prunenotes -g        # prune notes in all registered bundles recursively
