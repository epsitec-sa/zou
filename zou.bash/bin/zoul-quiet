#!/usr/bin/env bash

# Use the following statement to include current lib only once
#   [[ -z $ZOUL_QUIET ]] && . zoul-quiet
ZOUL_QUIET=true

export quiet=true

io_message() { :; }
io_message_unsafe() { :; }
mio_message() { :; }
mio_message_unsafe() { :; }
io_pause() { :; }
