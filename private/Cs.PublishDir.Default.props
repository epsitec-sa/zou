<?xml version="1.0" encoding="utf-8"?>
<!-- This property sheet defines the default 'PublishDir' property for C++ projects. -->
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <_PropertySheetDisplayName>[zou] PublishDir = pkg\$(Configuration)\</_PropertySheetDisplayName>
    <PublishDir Condition="'$(PublishDir)' == ''">pkg\$(Configuration)\</PublishDir>
  </PropertyGroup>
</Project>