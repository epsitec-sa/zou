<!-- This property sheet defines the default 'OutDir' property for go projects. -->
<Project>
  <PropertyGroup>
    <_PropertySheetDisplayName>[zou] OutDir = bin\$(Platform)\</_PropertySheetDisplayName>
    <OutDir Condition="'$(OutDir)' == ''">bin\$(Platform)\</OutDir>
  </PropertyGroup>
</Project>